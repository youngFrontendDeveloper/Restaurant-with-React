import React from "react";
import ReactDOM from "react-dom/client";
import Filter from "./components/Filter/Filter";

const root = ReactDOM.createRoot( document.querySelector( "#filter-wrap" ) );
root.render( <Filter/> );
